import { getFourLatestUserSummaries, getSummary, getUserSummaries, summarizeFile } from "./server"

const file = { summarizeFile, getSummary, getUserSummaries, getFourLatestUserSummaries}
export { file }